
import random

def algebra_q(x_value, coefficient, difficulty, multiplier):

    if difficulty == 0:
        response = "x {} {} = {}".format(operator, coefficient, x_value )
        answer =
    else:
        response = "{}x {} {} = {}".format(multiplier, operator, coefficient_1, coefficient_2)